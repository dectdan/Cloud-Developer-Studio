<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClaudeDevStudio.Dashboard.Views.PatternsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Patterns Explorer" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Proven approaches and anti-patterns" 
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                      Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <ItemsRepeater x:Name="PatternsList">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                               BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                               BorderThickness="1"
                               CornerRadius="8"
                               Padding="16"
                               Margin="0,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" 
                                             Text="{Binding Pattern}" 
                                             FontWeight="SemiBold"
                                             FontSize="16"
                                             TextWrapping="Wrap"/>

                                    <StackPanel Grid.Column="1" 
                                              Orientation="Horizontal" 
                                              Spacing="4"
                                              VerticalAlignment="Top">
                                        <FontIcon Glyph="{Binding Icon}" 
                                                FontSize="16"
                                                Foreground="{Binding IconColor}"/>
                                        <TextBlock Text="{Binding Confidence}" 
                                                 Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                 FontWeight="SemiBold"/>
                                        <TextBlock Text="%" 
                                                 Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Grid.Row="1" 
                                         Text="{Binding Type}"
                                         Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                         FontSize="12"
                                         Margin="0,4,0,0"/>

                                <StackPanel Grid.Row="2" Margin="0,8,0,0">
                                    <TextBlock Text="Applies to:" 
                                             FontSize="12"
                                             Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{Binding AppliesTo}" 
                                             FontSize="12"
                                             Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                             Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
